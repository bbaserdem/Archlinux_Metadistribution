# Maintainer: Batuhan Baserdem <baserdem.batuhan@gmail.com>
# vim: ft=PKGBUILD
pkgname=sbp-virtual
pkgver=4.0
pkgrel=1
pkgdesc="SBP Meta Package: Virtualization"
arch=('any')
url="https://github.com/bbaserdem/Arch"
licence=(MIT)
validpgpkeys=('0B7151C823559DD8A7A04CE36426139E2F4C6CCE')
source=('virtual.install')
sha256sums=(SKIP)
package() {
    install=virtual.install
    # Qemu setup
    depends+=(
        'docker'
        'qemu'
        'vde2'
        'ovmf'
        'libvirt'
        'ebtables'
        'dnsmasq'
        'bridge-utils'
        'virt-manager'
        'virtio-win')
}
